docker-compose up -d

# Conectar al contenedor:
docker exec -it mongodb mongosh

# Mostrar las BD:
show dbs

use bdwebs  # Cambiar a la BD

db.webs.find().pretty()  # Debería de listar todo el contenido del fichero!
