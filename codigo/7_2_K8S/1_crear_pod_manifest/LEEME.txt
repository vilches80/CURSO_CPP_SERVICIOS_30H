- Crear una imagen con Docker
docker build -t user/nginx:v1 .

- Crear el contenedor:
docker run -d -p 80:80 --name user/nginx:v1

- Parar
docker stop nginx

- Subirla a docker hub


- Alojarla en kubernetes y crear un pod